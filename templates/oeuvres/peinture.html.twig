{% extends 'base.html.twig' %}

{% block body %}
{% include "bandeauImg.html.twig" %}

<div class="container">

 <div class="container2">
    <div class="container3">
         <div class="container4">
           <nav aria-label="breadcrumb" class="fil">
              <ol class="breadcrumb">
                 <li class="breadcrumb-item"><a href="{{path('acceuil')}}">Accueil</a></li>
                 <li class="breadcrumb-item active" aria-current="page">Peinture</li>
              </ol>
          </nav>

            <div class="titre">
                <h3 class="colorYellow">peinture</h3>
            </div>
            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Totam autem quasi labore omnis animi ipsam accusamus exercitationem! Vero, cupiditate debitis tempore, repellendus mollitia quam molestiae rem rerum incidunt laborum ratione?
            Magnam deserunt debitis, consequuntur quam tenetur distinctio eaque, neque harum facilis .</p>
            <div class="oeuvre2" >
              	 {% for oeuvre in oeuvres  %}
               
                 {# <p>{{ oeuvre.titre}} </p> #}
              			{% if oeuvre.image %}
                  <div class="image2">
                         <a href=" {{ path('app_oeuvre_show', {'id': oeuvre.id}) }}">	<img src="{{ asset('images/oeuvre/' ~ oeuvre.image ) }}" alt="" class ="img300"></a>
                            <div class="sousImage">
                                <p>{{ oeuvre.titre}} </p>
                               <p > {% if oeuvre.description %}

                                {% if oeuvre.description|length > 30 %}
                                    {{ oeuvre.description|slice(0,29) }}...  
                                {% else %}
                                    {{ oeuvre.description }}
                                {% endif %}
                                {% endif %} 
                                    <p > <strong>{{oeuvre.prix}}€</strong></p>
                                    <div class="buttonSousImage">
                                    <button> <a href=" {{ path('app_oeuvre_show', {'id': oeuvre.id}) }}">voir plus</a> </button>
                                    </div>

                                </p> 
                      </div>
                    </div>
                     {% endif %}
                    
                     {% endfor %}  
            </div>
      </div>
    </div>
   <div style="font-size:2rem; display:flex;justify-content:center;">
     {{knp_pagination_render(oeuvres) }}
     </div>
</div>
                     





{% endblock %}